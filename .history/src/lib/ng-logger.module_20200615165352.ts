import { NgModule } from '@angular/core';
import { NgLoggerComponent } from './logger.component';

@NgModule({
  declarations: [NgLoggerComponent],
  imports: [
  ],
  exports: [NgLoggerComponent]
})
export class NgLoggerModule { }
